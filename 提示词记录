  # 服务器初始化脚本提示词
帮我按照以下内容写一个init_server.sh服务器管理脚本
1. 项目概述
目标：构建一个通用的、交互式的 Linux 服务器初始化脚本，实现“开箱即用”的标准化交付。
适用系统：CentOS, Rocky, AlmaLinux, Ubuntu, Debian, Kylin (麒麟), UOS (统信)。
核心原则：安全第一、交互灵活、操作可逆（有备份）、结果可视（有报告）。
2. 功能性需求 (Functional Requirements)
2.1 环境检测与预处理
表格
ID	需求描述	验收标准
F-01	Root 权限检查	非 Root 用户运行时必须报错并退出，提示使用 sudo。
F-02	操作系统识别	自动识别 OS 类型 (ID) 和版本，并打印日志；支持国产化系统标识。
F-03	包管理器识别	自动检测并使用 yum, dnf 或 apt，后续所有安装命令需适配当前管理器。
F-04	备份机制	修改任何配置文件前，必须自动备份原文件到 /root/init_backups_时间戳/ 目录。
F-05	日志记录	所有操作步骤、成功/失败状态、时间戳需实时写入 /var/log/server_init.log。
2.2 网络与主机身份配置
表格
ID	需求描述	验收标准
F-06	主机名配置	支持交互式输入新主机名，调用 hostnamectl 修改，并同步更新 /etc/hosts。
F-07	IP 模式开关	【关键】 必须询问“是否设置静态 IP”。
选 No：跳过所有 IP 配置，保持 DHCP/现状。
选 Yes：进入后续静态 IP 配置流程。
F-08	智能 IP 检测	若选静态 IP，自动检测并显示当前 IP 作为默认值参考。
F-09	静态参数配置	支持交互式输入 IP、网关、子网前缀 (Prefix)、DNS (主/备)。
F-10	网卡自适应	自动识别默认物理网卡名称，生成对应的配置文件 (ifcfg-* 或跳过 Netplan)。
F-11	网络重启与验证	应用配置后重启网络服务，并验证新 IP 是否生效；若失败需报错警告。
2.3 软件源管理
表格
ID	需求描述	验收标准
F-12	源配置开关	询问是否配置本地/内网源。
F-13	多模式支持	支持 ISO 镜像挂载 (自动 mount, 写 fstab, 建 repo) 和 HTTP 内网源 (替换 baseurl) 两种模式。
F-14	在线源重写	若不配内网源，可选是否重写在线源地址 (如阿里云/清华源),在线源地址用户自己手填,不要有默认值。
F-15	缓存更新	配置完成后自动执行 makecache 或 apt update 验证连通性。
2.4 基础组件安装
表格
ID	需求描述	验收标准
F-16	安装开关	【关键】 必须询问“是否安装基础组件”。
选 No：直接跳过，记录状态。
选 Yes：执行安装逻辑。
F-17	预定义列表	内置常用工具列表 (vim, wget, curl, net-tools, bash-completion, telnet, unzip, tar, gzip, chrony)。
F-18	容错处理	单个包安装失败不应中断脚本，需收集失败列表并在最后统一报告。
F-19	网络检测	若无本地源且外网不通 (ping 失败)，自动跳过安装并警告。
2.5 安全加固 (SSH)
表格
ID	需求描述	验收标准
F-20	加固开关	询问是否进行 SSH 安全加固。
F-21	管理员创建	交互式创建新管理员用户，配置 NOPASSWD sudo 权限，并强制设密码。
F-22	端口修改	交互式修改 SSH 端口 (默认 2222)，并注释掉旧端口配置。
F-23	Root 登录控制	交互式询问是否禁止 Root 远程登录 (PermitRootLogin no)。
F-24	密码认证控制	交互式询问是否禁止密码认证 (PasswordAuthentication no)，强制密钥登录。
F-25	延迟重启	SSH 配置修改后不立即重启服务，需等待防火墙配置完成后再统一重启，防止锁死。
2.6 防火墙配置
表格
ID	需求描述	验收标准
F-26	防火墙选型	支持选择 firewalld, ufw, iptables 或跳过。
F-27	冲突清理	启用选定防火墙时，自动 stop/disable 其他未选防火墙。
F-28	端口自动放行	必须自动将新的 SSH 端口加入白名单；支持额外输入业务端口。
F-29	服务联动	防火墙配置完成后，统一重启 SSH 服务，确保新端口生效。
2.7 系统优化与维护
表格
ID	需求描述	验收标准
F-30	时间同步	安装并启用 chrony 服务。
F-31	内核参数调优	修改 vm.swappiness (默认 10) 和 fs.file-max。
F-32	文件句柄数	修改 /etc/security/limits.conf，设置 nofile (默认 65535)。
F-33	数据盘自动化	扫描未挂载磁盘 -> 列表展示 -> 交互式选择 -> 格式化 (ext4) -> 挂载 -> 写 fstab。无盘时自动跳过。
2.8 交付与收尾
表格
ID	需求描述	验收标准
F-34	配置总结报告	【核心】 重启前必须打印详细报告，包含：主机名、IP 模式、IP、网关、组件安装状态、管理员、SSH 端口、安全策略、数据盘挂载点。
F-35	重启确认	询问是否立即重启。若选 Yes，倒计时 3 秒后 reboot；若选 No，提示手动重启。
3. 非功能性需求 (Non-Functional Requirements)
表格
类别	需求描述
安全性	1. 敏感操作 (格式化、网络变更) 必须有二次确认 (y/n)。
2. 脚本本身不硬编码密码，密码通过 passwd 命令交互式输入。
3. 备份文件需保留时间戳，防止覆盖。
健壮性	1. 遇到命令执行失败 (如网络重启失败) 不应直接崩溃退出，应记录错误并尝试继续后续步骤 (除非致命错误)。
2. 兼容不同版本的 Linux (CentOS 7/8/9, Ubuntu 20.04/22.04 等)。
易用性	1. 全程彩色输出 (Colorful Output)，区分 INFO, SUCCESS, WARN, ERROR。
2. 提供清晰的默认值提示 [默认：xxx]，支持直接回车跳过。
3. 关键步骤有 Emoji 图标引导，提升可读性。
可维护性	1. 代码结构模块化 (函数化)，每个功能块独立。
2. 全局配置变量集中在脚本头部，便于修改默认值。
3. 关键逻辑必须有详细注释。
